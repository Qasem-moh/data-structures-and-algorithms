'use strict';
let chNum = [1, 2, 3, 4, 5]
let rev = (arr) => {
    let NewA = [];
    for (let i = (arr.length - 1); i >= 0; i--) {
        arrNew.push(arr[i]);
    }
    return (NewA)
};
console.log(rev(chNum))
